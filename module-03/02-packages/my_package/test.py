# Define a function named 'test' that takes no parameters
def test(name, last_name):
    # Print a message to the console
    print(f"Hello! {name} {last_name}")


# Call the 'test' function to execute its content
